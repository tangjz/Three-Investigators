#include<bits/stdc++.h>
using namespace std;
#define pb push_back
#define mkp make_pair
#define fi first
#define se second
#define ll long long
#define M 1000000007
#define all(a) a.begin(), a.end()

void work(){
	int i = 1, m = strlen(ss + 1);
	for(; i <= m && s[i] == s[1]; ++i)
	 
}

int main(){
	scanf("%d", &T);
	while(T--){
		scanf("%d", &n);
		memset(b, 0, sizeof(b));
		for(int i = 0; i < n; ++i){
			scanf("%s", s + 1);
			m = strlen(s + 1);
			if(s[1] == '('){
				bool flag0 = 0, flag1 = 1;
				for(int i = 1; s[i] != ')'; ++i)
					
			}
			work(s);
		}
	}
	
#ifndef ONLINE_JUDGE
	system("pause");
#endif
	return 0;
}
